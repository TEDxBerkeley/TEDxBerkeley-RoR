.form-background

  %h1 New Event

  = form_for :event, url: events_path, html: {class: "new-form"} do |f|
    = f.text_field :theme, placeholder:"Theme"
    = f.date_select :date
    = f.text_area :description, placeholder:"Description"
    = f.submit "Submit"
